{
  "name": "@dvirtz/event-seeder",
  "type": "module",
  "main": "./dist/event-seeder.user.js",
  "imports": {
    "#*": "./src/*",
    "#tests/*": "./tests/*"
  },
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "vite --configLoader runner build",
    "build-watch": "yarn run build --watch",
    "lint": "eslint . --max-warnings 0",
    "check-types": "tsc --noEmit",
    "test": "playwright test --project=chromium",
    "record:har": "playwright test --max-failures=0 --project=chromium-har"
  },
  "dependencies": {
    "@repo/common-types": "workspace:*",
    "@repo/fetch": "workspace:*",
    "@repo/musicbrainz-ext": "workspace:*",
    "@repo/rxjs-ext": "workspace:*",
    "dom-mutations": "*",
    "rxjs": "*"
  },
  "devDependencies": {
    "@playwright/test": "*",
    "@repo/test-support": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@repo/vite-config": "workspace:*",
    "cross-env": "*",
    "eslint": "*",
    "semantic-release": "*",
    "typescript": "*",
    "vite": "*"
  }
}
