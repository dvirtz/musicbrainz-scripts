# Agent Guidance

- Keep Markdown files compliant with `.markdownlint-cli2.jsonc`.
- When running Playwright tests from agents, prefer a non-HTML reporter such as `--reporter=line` unless an HTML report is explicitly needed.
